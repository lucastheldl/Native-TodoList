import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  taskEmptyText: {
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    marginHorizontal: 20,
    marginVertical: 20,
  },
  taskInfoHeader: {
    flexDirection: "column",
    alignItems: "center",
    fontWeight: "bold",
    color: "#808080",
  },
  taskInfoText: {
    flexDirection: "column",
    alignItems: "center",
    color: "#808080",
  },
});
